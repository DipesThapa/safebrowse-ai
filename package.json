{
  "name": "safebrowse-ai",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "eslint src content.js --max-warnings=0",
    "zip:webstore": "mkdir -p dist && cd dist && rm -f extension.zip && zip -r extension.zip ../assets ../data ../src ../manifest.json ../LICENSE ../README.md -x ../src/content.js.bak ../content.js ../manifest.backup.json"
  },
  "devDependencies": {
    "@eslint/js": "^9.9.0",
    "eslint": "^9.9.0",
    "globals": "^15.8.0"
  }
}
